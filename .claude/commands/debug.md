Use datadog MCP server and prod DB querying (and also my local env to repro if needed) to debug this issue and find the root cause. Find logs/stack traces/etc as necessary. 

(Feel free to use cheaper subagents to query mcp/db/codebase, but use full intelligence for planning + heavy thinking about interactions)

After you are confident in a finding, spawn ADVERSARIAL SUBAGENTS to poke holes and see if there are inconsistencies and contradicting evidence and alternative explanations.

Once you have finalized your root cause after adversarial review, create a new worktree off latest main branch and make a PR.

