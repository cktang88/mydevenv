Use datadog's or betterstack's (whichever is installed here) MCP server and prod DB querying (and also my local env to repro if needed) to debug this issue and find the root cause. Find logs/stack traces/etc as necessary. 

After you are confident in a finding, spawn ADVERSARIAL SUBAGENTS to poke holes and see if there are inconsistencies and contradicting evidence and alternative explanations.
